{
  "name": "open-curriculum-chatgpt-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/app.js && node --check worker/lib.js && node --check worker/index.js && node --check scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm run test && npm run build"
  }
}
